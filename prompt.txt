Project Title:
Travel Bucket List & Expedition Planning Platform
Tech Stack:
Frontend: Next.js + Tailwind CSS
Backend: Node.js + Express
Database: MySQL (local)
Given Database & Connection (MUST BE USED AS-IS)
✅ Database Connection
const pool = mysql.createPool({
  host: process.env.DB_HOST || 'localhost',
  user: process.env.DB_USER || 'root',
  password: process.env.DB_PASSWORD || '9709303105',
  database: process.env.DB_NAME || 'travel_bucket_list',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0
});

Use the above connection from db.js for all queries via pool.query.
Do not modify connection credentials or logic — just import and use it.
SQL TABLES (Do Not Modify Schema)
CREATE TABLE User (
    UserID INT PRIMARY KEY AUTO_INCREMENT,
    Name VARCHAR(100) NOT NULL,
    Email VARCHAR(100) UNIQUE NOT NULL,
    Phone VARCHAR(15),
    Street VARCHAR(100),
    City VARCHAR(50),
    State VARCHAR(50),
    Pincode VARCHAR(10)
);

CREATE TABLE TripPackage (
    PackageID INT PRIMARY KEY AUTO_INCREMENT,
    Title VARCHAR(100) NOT NULL,
    Description TEXT,
    Price DECIMAL(10,2) NOT NULL,
    StartDate DATE,
    EndDate DATE,
    Duration INT GENERATED ALWAYS AS (DATEDIFF(EndDate, StartDate)) STORED
);

CREATE TABLE Hotel (
    HotelID INT PRIMARY KEY AUTO_INCREMENT,
    HotelName VARCHAR(100) NOT NULL,
    City VARCHAR(50),
    State VARCHAR(50),
    Country VARCHAR(50),
    Rating DECIMAL(2,1),
    ContactNumber VARCHAR(15)
);

CREATE TABLE Transport (
    TransportID INT PRIMARY KEY AUTO_INCREMENT,
    Type ENUM('Bus','Flight','Train','Cab') NOT NULL,
    ProviderName VARCHAR(100),
    Capacity INT,
    PackageID INT,
    FOREIGN KEY (PackageID) REFERENCES TripPackage(PackageID)
);

CREATE TABLE Booking (
    BookingID INT PRIMARY KEY AUTO_INCREMENT,
    UserID INT,
    PackageID INT,
    HotelID INT,
    BookingDate DATE NOT NULL,
    Status ENUM('Confirmed','Pending','Cancelled') DEFAULT 'Pending',
    TotalAmount DECIMAL(10,2),
    FOREIGN KEY (UserID) REFERENCES User(UserID),
    FOREIGN KEY (PackageID) REFERENCES TripPackage(PackageID),
    FOREIGN KEY (HotelID) REFERENCES Hotel(HotelID)
);

CREATE TABLE Payment (
    PaymentID INT PRIMARY KEY AUTO_INCREMENT,
    BookingID INT,
    Mode ENUM('UPI','Card','NetBanking'),
    Amount DECIMAL(10,2) NOT NULL,
    Status ENUM('Success','Failed','Pending') DEFAULT 'Pending',
    Date DATE,
    FOREIGN KEY (BookingID) REFERENCES Booking(BookingID)
);

CREATE TABLE Review (
    ReviewID INT PRIMARY KEY AUTO_INCREMENT,
    UserID INT,
    HotelID INT,
    Rating INT CHECK (Rating BETWEEN 1 AND 5),
    Comment TEXT,
    Date DATE,
    FOREIGN KEY (UserID) REFERENCES User(UserID),
    FOREIGN KEY (HotelID) REFERENCES Hotel(HotelID)
);

CREATE TABLE Admin (
    AdminID INT PRIMARY KEY AUTO_INCREMENT,
    Name VARCHAR(100),
    Email VARCHAR(100),
    Role ENUM('SuperAdmin','Manager','Support'),
    HotelID INT,
    FOREIGN KEY (HotelID) REFERENCES Hotel(HotelID)
);

CREATE TABLE Destination (
    DestinationID INT PRIMARY KEY AUTO_INCREMENT,
    Name VARCHAR(100),
    City VARCHAR(50),
    State VARCHAR(50),
    Country VARCHAR(50),
    PopularityIndex INT,
    PackageID INT,
    FOREIGN KEY (PackageID) REFERENCES TripPackage(PackageID)
);

🧩 Backend Requirements (Node.js + Express)
✅ Architecture
backend/
├─ controllers/       # Business logic using JOINs
├─ models/            # SQL query definitions
├─ routes/            # API endpoints
├─ middleware/        # Auth, validation, error handling
├─ db.js              # Provided MySQL pool connection
└─ server.js

✅
Packages API
GET /api/packages → List all packages
GET /api/packages/:id → Get package details with related destinations and transport (JOIN Destination, Transport)
POST/PUT/DELETE for admin management
Bookings API
GET /api/bookings/:userId → Fetch user bookings with related package & hotel info (JOIN User, TripPackage, Hotel)
POST /api/bookings → Create a new booking
Update status, cancel booking, etc.
Payments API
GET /api/payments → Join Payment + Booking + User to show full payment info
POST /api/payments → Add a payment record
Reviews API
GET /api/reviews/:hotelId → Join Review + User for reviewer name
POST /api/reviews → Add user review
Admin API
GET /api/admin/analytics → Use SQL JOIN + GROUP BY
Example analytics:
Total bookings per destination
Most popular packages
Revenue per hotel (JOIN Booking, Payment)
Average rating per hotel (AVG(Rating))
✅ JOIN Query Examples (Expected in Controllers)
Use SQL JOINs to combine related tables, e.g.:

SELECT b.BookingID, u.Name AS UserName, p.Title AS PackageName, h.HotelName, b.Status, b.TotalAmount
FROM Booking b
JOIN User u ON b.UserID = u.UserID
JOIN TripPackage p ON b.PackageID = p.PackageID
JOIN Hotel h ON b.HotelID = h.HotelID;


or

SELECT d.Name AS Destination, t.Type AS TransportType, t.ProviderName, p.Title AS PackageTitle
FROM Destination d
JOIN TripPackage p ON d.PackageID = p.PackageID
JOIN Transport t ON p.PackageID = t.PackageID;


Every controller that fetches multi-table data must use proper JOINs, GROUP BY, and aggregates (COUNT, AVG, SUM) where applicable.

🎨 Frontend Requirements (Next.js + Tailwind CSS)
Pages
/login & /register
/dashboard – Show summary (bookings, payments, ratings)
/packages – Display trip packages with filters
/bookings – User bookings list with JOINed hotel/package data
/admin – CRUD panels for packages, hotels, destinations
/map – Interactive Journey Highlights Map (Google Maps or Mapbox)
Features
Tailwind CSS for modern, responsive UI
Dynamic modals & cards
Tables with hover sorting and pagination
Search & filter inputs
Map integration for destinations
Dashboard analytics with chart.js or recharts
State management with Context or Zustand
🔐 Security & Standards
ES6 modular syntax
Input validation & error handling
Use of environment variables (.env)
Optional JWT-based authentication for admins
CORS enabled for frontend-backend connection
Clean, human-readable, commented code
🗂️ Full Directory Structure
project-root/
├─ backend/
│  ├─ controllers/
│  ├─ routes/
│  ├─ middleware/
│  ├─ models/
│  ├─ db.js
│  └─ server.js
│
├─ frontend/
│  ├─ pages/
│  ├─ components/
│  ├─ styles/
│  └─ public/
│
├─ sql/
│  └─ init.sql  # (the above schema)
│
├─ package.json
└─ README.md

📊 Expected JOIN-Based Features
Dashboard Analytics (Admin Panel)
Most popular hotels → JOIN Review + Hotel
Top destinations → JOIN Booking + Destination + Package
Total revenue per package → JOIN Payment + Booking + Package
Average trip duration per city → JOIN Destination + Package
User Dashboard
Joined booking, payment, and package info
Filter bookings by status or hotel
Map Page
Fetch all destinations with their package and hotel details (via JOIN)
Show on map with clickable markers
✅ Final Deliverables
Next.js Frontend (Tailwind + Interactive UI)
Node.js Backend (REST APIs using JOIN queries)
Integration with provided SQL & db.js
Documentation (README.md setup + API reference)
Interactive Map & Analytics dashboard
🧩 Goal
Deliver a fully functional full-stack application that uses JOIN-based SQL queries for relational insights, built around the given MySQL schema and db connection, with an interactive, professional, maintainable UI and a clean modular backend.